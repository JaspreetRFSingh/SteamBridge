# Custom Connector Development

## Building a Source Connector
1. Extend `SourceConnector` class
2. Implement `SourceTask` for polling logic
3. Define configuration with `ConfigDef`
4. Package as JAR with dependencies
